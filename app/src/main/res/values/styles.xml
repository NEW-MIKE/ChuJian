<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="NormalDialogStyle">
        <!--对话框背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--边框 -->
        <item name="android:windowFrame">@null</item>
        <!--没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!--背景透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否有覆盖 -->
        <item name="android:windowContentOverlay">@null</item>
        <!--背景变暗-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--设置横屏最少宽度-->
        <item name="android:windowMinWidthMajor">50dp</item>
    </style>
    <style name="Widget.SeekBar.Normal" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">3.0dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">3.0dip</item>
        <item name="android:thumbOffset">0dip</item>
        <item name="android:progressDrawable">@drawable/seekbar_heating_bar</item>
        <item name="android:thumb">@drawable/seekbar_heating_circle</item>
    </style>
    <style name="LoadingDialog">
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowBackground">@android:color/transparent</item><!--背景透明-->
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <style name="XSpinnerStyle" parent="android:Widget.ListView.DropDown">
        <!-- 分隔线颜色 -->
        <item name="android:divider">@color/all_blue_color</item>
        <item name="android:dividerHeight">1px</item>
    </style>
    <style name="ProgressBar" parent="android:style/Widget.Holo.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/dialog_loading</item>
        <item name="android:indeterminateDuration">50</item>
    </style>
    <style name="recy_vertical_style">
        <!--滚动条宽度-->
        <item name="android:scrollbarSize">@dimen/rv_scroller_width</item>
        <!--滚动条方向-->
        <item name="android:scrollbars">vertical</item>
        <!--滚动条是否滑动-->
        <item name="android:fastScrollEnabled">true</item>
        <!--滚动条不消失-->
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <!--滚动条滑动块颜色-->
        <item name="android:scrollbarThumbVertical">@color/grey</item>
        <!--滚动条默认颜色-->
        <item name="android:scrollbarTrackVertical">@color/rv_scroller_bar_bg</item>
    </style>

</resources>